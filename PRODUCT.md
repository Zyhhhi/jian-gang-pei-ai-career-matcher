# 简历分析｜AI 简历-JD 匹配工具

## 产品定位

简历分析是一个面向应届生和初级求职者的 AI 求职辅助产品。用户在投递岗位前，将简历文本和目标岗位 JD 粘贴到页面中，系统基于 DeepSeek 文本 API 生成结构化匹配分析，帮助用户判断匹配度、发现简历短板，并形成可执行的修改建议。

这个项目不是为了包装成“万能求职工具”，而是展示一个 AI 产品经理候选人如何基于真实 API 能力设计可上线、可复用、可迭代的产品闭环。它作为新版独立项目发布，用于和原始简岗配 Demo 形成对比。

## 目标用户

- 正在投递岗位的应届生
- 想转向 AI 产品经理 / AI 产品助理的求职者
- 缺少简历与 JD 结构化判断方法的初级求职者

## 核心痛点

1. 不知道自己的简历和岗位 JD 是否匹配。
2. 不知道岗位真正看重哪些能力。
3. 不知道简历应该优先修改哪里。
4. 投递前缺少结构化判断和行动建议。
5. AI 输出如果只是大段文字，难以保存和复用。

## 能力边界

当前版本只接入 DeepSeek 文本 API，因此产品能力边界必须清晰：

- 支持：简历文本与岗位 JD 文本分析。
- 支持：结构化结果、历史记录、Markdown 导出、反馈记录、Mock 兜底。
- 不支持：图片简历识别。
- 不支持：截图内容识别。
- 不支持：PDF 文件自动解析。
- 不承诺：自动保证通过筛选、替代 HR 判断、100% 准确匹配。

如果用户手里是 PDF 或 Word 简历，建议先复制其中的文字，再粘贴到输入框中分析。

## AI Agent 流程

1. 用户粘贴简历文本和岗位 JD。
2. 系统校验输入完整度。
3. DeepSeek 文本模型提取岗位核心要求。
4. 对比简历内容，生成匹配评分、优势、短板和缺失关键词。
5. 输出简历修改建议、面试可能问题、HR 打招呼话术和下一步行动。
6. 分析结果保存到本地历史记录。
7. 用户可以复制、导出、重新生成或提交反馈。

## MVP 功能

- 简历文本输入
- 岗位 JD 输入
- DeepSeek API Key 本地保存
- API 失败后的 Mock 兜底
- 结构化分析结果
- 历史记录
- 删除历史记录
- Markdown 导出
- 一键复制结果
- 用户反馈记录
- 示例数据
- 移动端适配

## 正式产品体验要求

功能可以保持 MVP 阶段，但视觉、信息架构和交互体验必须按照正式 AI 产品标准设计。

页面不能做成“能跑就行”的学生 Demo 或技术实验。用户第一次打开页面时，应该能感受到这是一个真实可用的 AI 求职产品：定位清晰、主流程明确、信息层级稳定、交互反馈完整、视觉表达克制可信。

因此后续优化要优先满足：

1. 首屏要像正式产品入口，而不是本地测试页。
2. 页面文案要让用户快速理解产品解决什么问题。
3. 输入、分析、结果、历史、导出、反馈要形成完整闭环。
4. 视觉风格要简洁、高级、可信，避免技术大屏和学生作业感。
5. 空状态、加载状态、失败状态和 Mock 兜底都要有清晰反馈。
6. 面向 HR 和面试官展示时，要能体现真实 AI 产品的完整度。

## 产品设计原则

1. 产品表达必须符合真实模型能力，不夸大 DeepSeek 文本 API。
2. 输出必须结构化，避免只返回一大段 AI 文本。
3. API 不可用时演示不能中断，必须有 Mock 兜底。
4. 所有 AI 结论仅作为求职准备参考，保留人工复核空间。
5. 功能闭环优先于视觉炫技。
6. 页面文案要让 HR 和面试官能快速理解用户、痛点、方案和价值。

## 作品集价值

这个项目可以体现以下 AI 产品经理能力：

- 需求拆解：从求职者投递前判断困难出发设计功能。
- Agent 流程设计：将输入、分析、结构化输出、保存、反馈串成闭环。
- Prompt 设计：约束模型只基于用户粘贴文本输出 JSON。
- 风险意识：明确 DeepSeek 不支持多模态，不夸大产品能力。
- MVP 意识：用 localStorage、Mock 和静态部署完成可演示版本。
- 面试表达：能讲清楚项目从 Demo 到 MVP 的升级过程。

## 后续迭代方向

1. 增加可编辑结果，让用户在 AI 建议基础上二次修改。
2. 增加后端代理，避免前端直接调用 API Key。
3. 将历史记录和反馈从 localStorage 升级为数据库。
4. 增加更多岗位模板，例如 AI 产品助理、数据产品、运营产品。
5. 增加分析质量反馈面板，用真实用户反馈继续优化 Prompt。
