# 简岗配 · RoleFit AI v0.8.6C-H 发布记录

计划发布日期：2026-07-20

## 本次修复

- 全面替换页面示例数据，所有候选人、学校、公司、项目和经历均使用明显虚构内容。
- “填入示例”只覆盖当前页面临时表单，不自动保存简历档案或岗位草稿。
- 示例填入时清空旧岗位补充字段、截图元数据、分析结果和自带 Key 直传确认状态。
- 示例简历必须由用户主动保存，示例 JD 必须由用户主动确认；完成前禁止开始分析。
- 认证 scope 恢复完成前禁用示例按钮，防止示例临时态与账号档案相互覆盖。
- 示例 JD 确认时使用完整替换语义，不合并旧岗位草稿字段。

## 隐私验证

- 使用合成隐私哨兵覆盖 Guest、账号 A、账号 B、历史与 legacy quarantine 场景。
- 契约测试确认示例填充不读取或写入 scoped storage，且请求体只使用用户明确保存和确认的数据。
- 当前源码不包含本次移除的身份和经历内容。

## 安全状态

- 前端 `ENABLE_PLATFORM_AI=false` 保持关闭。
- Worker `PLATFORM_AI_ENABLED=false` 保持关闭，本次不部署 Worker。
- 本次不调用 DeepSeek、Worker 或 Supabase，不执行 migration。
